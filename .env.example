# Variáveis públicas do Expo (disponíveis em process.env)
# Substitua pela URL do seu backend hospedado (Render, etc.)
# Inclua o sufixo /api se sua API usa esse prefixo nos endpoints
EXPO_PUBLIC_API_URL=https://app-catolico.onrender.com
